import Mask from '../../common/controls/mask/mask';

const TestMask = () => {
  return (
    <div
      style={{
        height: 200,
        width: 200,
        display: 'flex',
        border: '1px solid black',
        margin: '50%',
      }}
    >
      <Mask />
    </div>
  );
};

export default TestMask;
