export enum JsonValueType {
  none,
  null,
  undefined,
  boolean,
  number,
  string,
  array,
  object,
  function,
}

export enum Type {
  default = "default",
  primary = "primary",
  secondary = "secondary",
  danger = "danger",
}

export enum Size {
  small = "small",
  medium = "medium",
  large = "large",
}
