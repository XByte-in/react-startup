export enum ValueType {
  none,
  null,
  undefined,
  boolean,
  number,
  string,
  array,
  object,
  function,
}

export enum Size {
  xtraSmall,
  small,
  medium,
  large,
  xtraLarge
}

export enum Type {
  default,
  primary,
  secondary,
  tertiary,
  danger
};