export enum JsonValueType {
  none,
  null,
  undefined,
  boolean,
  number,
  string,
  array,
  object,
  function,
}

export enum Type {
  default,
  primary,
  secondary,
  tertiary,
  danger
};

export enum Size {
  small,
  medium,
  large
};