export enum Type {
  primary = 'primary',
  secondary = 'secondary',
  default = 'default',
  danger = 'danger',
}

export enum Size {
  small = 'small',
  medium = 'medium',
  default = 'default',
  large = 'large',
}

export enum DateTimePickerType {
  time,
  dateTime,
  date,
  dateRange,
  month,
  year,
}
